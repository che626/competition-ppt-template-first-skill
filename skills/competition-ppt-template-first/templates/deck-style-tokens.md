# Deck Style Tokens

Complete this once per deck before generating any page template. Treat these values as the deck's visual source of truth. Do not introduce one-off colors, type sizes, frame treatments, or spacing values on later slides without updating this file and recording why.

```md
## Deck identity

- Project / deck:
- Token version:
- Subject-derived visual rationale:
- Reference pages or visual sources:

## Color tokens

| Token | Hex / description | Role | Permitted use |
| --- | --- | --- | --- |
| `canvas-dark` |  | dark template base | dense evidence pages / dark hero scenes |
| `canvas-light` |  | light template base | readable technical or document pages |
| `ink-primary` |  | primary text | titles / main claims |
| `ink-secondary` |  | supporting text | explanations / captions |
| `accent-primary` |  | primary emphasis | one key conclusion / active evidence |
| `accent-support` |  | secondary emphasis | one subordinate grouping only |
| `signal-warning` |  | caution / trade-off | verified risk or limitation only |

## Typography tokens

| Token | Font / weight | Size range | Role | Maximum use |
| --- | --- | --- | --- | --- |
| `display` |  |  | cover / closing statement | one per page |
| `title` |  |  | page title | one per page |
| `claim` |  |  | main conclusion | one per page |
| `section` |  |  | evidence block heading | 2-4 per page |
| `body` |  |  | explanatory copy | defined minimum required |
| `caption` |  |  | source / image caption | evidence-attached only |

## Spatial tokens

| Token | Value | Rule |
| --- | --- | --- |
| `edge-margin` |  | minimum safe distance from slide edges |
| `group-gap` |  | minimum gap between unrelated groups |
| `inner-gap` |  | gap inside one content group |
| `title-origin` |  | shared title / page-number origin |
| `caption-offset` |  | distance between evidence and its caption |

## Image and surface tokens

- Template contrast family: [light / mid / dark distribution]
- Template material / lighting rule:
- Real-evidence frame treatment:
- Screenshot framing treatment:
- Decorative cutout treatment:
- Border / radius / shadow rule:
- Forbidden treatments:

## Page rhythm

- Rich-scene pages:
- Quiet-evidence pages:
- Planned structural breaks:
- Maximum consecutive uses of one layout family: 2

## Change log

| Version | Change | Why it changed | Affected slides |
| --- | --- | --- | --- |
|  |  |  |  |
```
